header:
  version: 2

machine:  iot2000

repos:
  meta-iot2000:
    layers:
      meta-iot2000-bsp:

  poky:
    url: https://git.yoctoproject.org/git/poky
    refspec: f7b90ab3eaf832bd81f3efc1dab4dcf6863ac284
    layers:
      meta:
      meta-poky:
      meta-yocto-bsp:

  meta-intel:
    url: https://git.yoctoproject.org/git/meta-intel
    refspec: 5adbf6df4fd89e7531ccccfb9cec7a5314d635f0

  meta-openembedded:
    url: http://git.openembedded.org/meta-openembedded
    refspec: e08670d4362b57373557fbe6b31cbffffa465acc
    layers:
      meta-oe:

bblayers_conf_header:
  standard: |
    POKY_BBLAYERS_CONF_VERSION = "2"
    BBPATH = "${TOPDIR}"
    BBFILES ?= ""

local_conf_header:
  standard: |
    CONF_VERSION = "1"

  debug-tweaks: |
    EXTRA_IMAGE_FEATURES = "debug-tweaks"

  diskmon: |
    BB_DISKMON_DIRS = "\
        STOPTASKS,${TMPDIR},1G,100K \
        STOPTASKS,${DL_DIR},1G,100K \
        STOPTASKS,${SSTATE_DIR},1G,100K \
        STOPTASKS,/tmp,100M,100K \
        ABORT,${TMPDIR},100M,1K \
        ABORT,${DL_DIR},100M,1K \
        ABORT,${SSTATE_DIR},100M,1K \
        ABORT,/tmp,10M,1K"

  image_features: |
    EXTRA_IMAGE_FEATURES += "eclipse-debug debug-tweaks dbg-pkgs ssh-server-openssh"

  extra_mirror_options: |
    # ADAPT THIS TO THE WORKSHOP NETWORK !!
    MIRRORHOST = "builder"
    PREMIRRORS =+ "\
    git://.*/.* http://${MIRRORHOST}/downloads/ \n \
    ftp://.*/.* http://${MIRRORHOST}/downloads/ \n \
    http://.*/.* http://${MIRRORHOST}/downloads/ \n \
    https://.*/.* http://${MIRRORHOST}/downloads/ \n \
    "
    BB_GENERATE_MIRROR_TARBALLS = "1"
    SSTATE_MIRRORS += " \
    file://.* http://${MIRRORHOST}/sstate-mirror/${MACHINE}/PATH  \n \
    "
    DL_DIR = "/home/student/.yocto/downloads/"
    SSTATE_DIR = "/home/student/.yocto/sstate-cache/${MACHINE}/"
    INHERIT += "rm_work"
    BB_FETCH_PREMIRRORONLY = "1"
    BB_NO_NETWORK = "1"
    SSTATE_MIRROR_ALLOW_NETWORK = "1"



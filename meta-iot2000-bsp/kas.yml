header:
  version: 2

machine:  iot2000

repos:
  meta-iot2000:
    layers:
      meta-iot2000-bsp:

  poky:
    url: https://git.yoctoproject.org/git/poky
    refspec: 65d23bd7986615fdfb0f1717b615534a2a14ab80
    layers:
      meta:
      meta-poky:
      meta-yocto-bsp:

  meta-intel:
    url: https://git.yoctoproject.org/git/meta-intel
    refspec: d68dda959b5bf6f90aa8e5c2f027be324bb68e14

  meta-openembedded:
    url: http://git.openembedded.org/meta-openembedded
    refspec: 6e3fc5b8d904d06e3aa77e9ec9968ab37a798188
    layers:
      meta-oe:

bblayers_conf_header:
  standard: |
    POKY_BBLAYERS_CONF_VERSION = "2"
    BBPATH = "${TOPDIR}"
    BBFILES ?= ""

local_conf_header:
  standard: |
    CONF_VERSION = "1"

  debug-tweaks: |
    EXTRA_IMAGE_FEATURES = "debug-tweaks"

  diskmon: |
    BB_DISKMON_DIRS = "\
        STOPTASKS,${TMPDIR},1G,100K \
        STOPTASKS,${DL_DIR},1G,100K \
        STOPTASKS,${SSTATE_DIR},1G,100K \
        STOPTASKS,/tmp,100M,100K \
        ABORT,${TMPDIR},100M,1K \
        ABORT,${DL_DIR},100M,1K \
        ABORT,${SSTATE_DIR},100M,1K \
        ABORT,/tmp,10M,1K"

  image_features: |
    EXTRA_IMAGE_FEATURES += "eclipse-debug debug-tweaks dbg-pkgs ssh-server-openssh"

  extra_mirror_options: |
    DL_DIR = "/home/student/.yocto/downloads/"
    SSTATE_DIR = "/home/student/.yocto/sstate-cache/${MACHINE}/"
    BB_GENERATE_MIRROR_TARBALLS = "1"
